<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Readle" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Splash Screen (Android 12+) -->
        <!-- Use icon with padding to prevent circular cropping -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_safe</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
    </style>
</resources>

